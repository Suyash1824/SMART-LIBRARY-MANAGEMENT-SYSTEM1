# SMART-LIBRARY-MANAGEMENT-SYSTEM1
Smart Library Management System is a DBMS-based project that automates book issuing and returns in a college library. It tracks due dates, sends email reminders two days before deadlines, and calculates overdue fines automatically. Built using MySQL and Java, it demonstrates tables, triggers, stored procedures, and a user-friendly interface.
